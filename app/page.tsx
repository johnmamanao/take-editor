import Landing from './landing';
export const metadata = {
  title: 'Take. Screen recordings, ready to share.',
  description:
    'Trim the pauses, frame your app, and zoom into what matters. Edit screen recordings in your browser with Take.',
};
export default function Page() {
  return <Landing />;
}
